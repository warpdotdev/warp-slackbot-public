# Slack Bot Token (starts with xoxb-)
#Go to "OAuth & Permissions"
#Copy the "Bot User OAuth Token" (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack App Token (starts with xapp-)
#Go to "Basic Information" → "App-Level Tokens"
#Click "Generate Token and Scopes"
#Add scope: connections:write
SLACK_APP_TOKEN=xapp-your-app-token-here

# GitHub Access Token (for private repos)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)
GITHUB_TOKEN=ghp_your-github-token-here

#Required for agent 
#https://docs.warp.dev/developers/cli#generating-api-keys
WARP_API_KEY=your_token_here
#Notebook ID, Find by referencing using @ symbol and select
# the id following the notebook prefix<notebook:ID> 
NOTEBOOK_ID=test

# Docker Architecture for Warp CLI installation
# Set to 'arm64' for ARM processors (Apple Silicon, etc.)
# Set to 'amd64' for Intel/AMD processors (default)
DOCKER_ARCH=amd64
